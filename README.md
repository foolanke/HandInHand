# Douling-for-ASL

## Running Backend
python3.11 -m venv env
source env/bin/activate
pip install -r requirements.txt

## Running Frontend
cd frontend
npm install
npm run dev


#MAKE SURE that names match the same on frontend backend as they literally are used for files